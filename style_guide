:root {
  --color-green: #4a666b;
  --color-black: #180222;
  --color-light-blue: #c3e7ef;
  --color-yellow: #e6edaa;
  --color-blue: #405aa8;
}

/* editing squarespace defaults */

.header-nav-list .header-nav-item:not(:first-child) {
  border-left: 1px solid var(--color-blue);
  margin-left: 1vw !important;
  padding-left: 1vw !important;
}

/* pages */

/* classes */

.space_desc_box {
  /* box with dashed edges- used in Spaces page */
  border: 1px dashed;
  padding: 1% 3% 2% 3%;
  height: 475px;
  font-size: 80%;
  background: white;

  h3 {
    margin: 0.5em auto 0.2em auto;
    color: var(--color-blue);
  }

  p2 {
    font-size: 110%;
  }

  ul {
    margin: 0.4em auto 0.4em -0.5em;
    line-height: -1em;
    font-size: 110%;
  }

  button {
    font-size: 110%;
    margin: 1% 25% 2% 28%;
    color: white;
    background-color: var(--color-blue);
    padding: 2% 8% 2% 8%;
    top: 90%;
    border: none;
    white-space: nowrap;
    overflow: hidden;
  }
}

.studio_card {
  /* Space name/type cards to link to indvidual space pages for Book Now page */
  width: 100%;
  height: 450px;
  background: linear-gradient(to bottom, var(--color-blue) 40%, var(--color-blue) 54%, var(--color-light-blue) 50%, var(--color-light-blue) 100%);
  position: relative;
  box-shadow: 2px 2px 7px 1px #C0C0C0;

  >* {
    margin: 0;
    display: flex;
    color: white;
  }

  .img1 {
    object-fit: cover;
    height: 40%;
    width: 100%;
  }

  h1 {
    font-size: 25px;
    color: white;
    padding: 4% 0% 5% 5%;
    display: inline-block;
  }

  table {
    position: absolute;
    font-size: 15px;
    display: inline-block;
    padding-left: 20%;
    padding-top: 1%;
    white-space: nowrap;
    overflow: hidden;
    line-height: 1.5em;
  }

  table td {
    padding: 2px;
  }

  .tablewide {
    padding-left: 20%;
  }

  .middle {
    width: 99%;
    height: 20%;
    display: flex;
    justify-content: center;
    padding-top: 2%;
    color: black;
    font-size: 18px;
  }

  .middle img {
    max-width: 100%;
    height: 50px;
  }

  .middle .icon_item {
    max-height: 80%;
    display: inline-block;
    text-align: center;
    flex: 1;
  }

  .middle .icon_item span {
    display: block;
    padding-top: 5%;
    line-height: 1.5em;
  }

  .line {
    width: 90%;
    height: 1px;
    background: var(--color-blue);
    position: absolute;
    top: 84%;
    margin-left: 5%;
  }

  button {
    font-size: 20px;
    font-weight: 300;
    margin: 12% 12% 15% 15%;
    color: white;
    background-color: var(--color-blue);
    padding: 2% 10% 2% 10%;
    border: none;
    white-space: nowrap;
    overflow: hidden;
  }

  .buttonwide {
    margin-top: 9%;
    margin-left: 19%;
  }
}
